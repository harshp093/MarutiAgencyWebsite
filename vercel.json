{
  "buildCommand": "pnpm --filter @maaruti/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install -g pnpm@latest && pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
